"""Present arbitrary lat/lon points to a model as a regular lat/lon grid.

This is designed to abstract away the lat/lon points and as a layer that can be put in
front of models that more expect a regular lat/lon grid as input.
"""
